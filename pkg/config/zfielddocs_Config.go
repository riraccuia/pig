// Code generated by gen_struct_tags; DO NOT EDIT.

package config

import (
	"fmt"

	"github.com/riraccuia/pig/pkg/common"
)

func (Config) DescribeFields() map[string]common.FieldMeta {
	return map[string]common.FieldMeta{
		"adapter":                                      {Optional: true, Desc: "Adapter configuration block."},
		"adapter.bind_adapter":                         {Optional: true, Desc: "Adapter name to bind to, e.g. \"eth0\". Only required when 'proto' is tls-in-icmp."},
		"adapter.mtu":                                  {Default: fmt.Sprint(DefaultMTU), Desc: "Maximum Transmission Unit."},
		"adapter.queue_size":                           {Default: fmt.Sprint(DefaultQueueSize), Optional: true, Desc: "Size of the network queues for the tunnel."},
		"adapter.tunnel_address":                       {Desc: "CIDR format."},
		"log":                                          {Optional: true, Desc: "Logging configuration block."},
		"log.file":                                     {Optional: true, Desc: "Path to the log file to write to. Otherwise logs to stdout."},
		"log.level":                                    {Default: fmt.Sprint("info"), Optional: true, Desc: "Log level. Use one of \"trace\", \"debug\", \"info\", \"warn\", \"error\"."},
		"log.rotate_size":                              {Optional: true, Desc: "Size of the log file to rotate at. e.g. 100k, 1m, 1g, or actual size in bytes."},
		"mqtt_global":                                  {Optional: true, Desc: "Default MQTT broker configuration for NAT traversal."},
		"mqtt_global.address":                          {Default: fmt.Sprint(DefaultICEBrokerAddress), Desc: "MQTT broker address in the form of mqtt://host:port or ssl://host:port."},
		"mqtt_global.client_id":                        {Optional: true, Desc: "MQTT client ID."},
		"mqtt_global.password":                         {Optional: true, Desc: "MQTT password."},
		"mqtt_global.username":                         {Optional: true, Desc: "MQTT username."},
		"route":                                        {Optional: true, Desc: "Route configuration block."},
		"route.enabled":                                {Default: fmt.Sprint(false), Optional: true, Desc: "Enable route configuration."},
		"route.routes":                                 {Optional: true, Desc: "Routes are the routes that will be used for the routing."},
		"route.routes[].destination":                   {Desc: "Destination CIDR to route. E.g. \"192.168.1.0/24\"."},
		"route.routes[].gateway":                       {Optional: true, Desc: "For \"static\" routes, the gateway IP address to use for the route."},
		"route.routes[].interface":                     {Optional: true, Desc: "For \"static\" routes, the interface name to use for the route."},
		"route.routes[].type":                          {Desc: "Type of route. One of \"tunnel\", \"bypass\", \"static\"."},
		"script_path":                                  {Optional: true, Desc: "Path to an executable file to call on various tunnel events."},
		"stun_global":                                  {Default: fmt.Sprint(DefaultICESTUNServer), Optional: true, Desc: "Default STUN server address for NAT traversal in the form of host:port."},
		"tunnels":                                      {Desc: "Tunnel configuration block."},
		"tunnels[].adapter":                            {Optional: true, Desc: "Adapter configuration specific for this tunnel. Mandatory when the direction is \"listen\"."},
		"tunnels[].adapter.bind_adapter":               {Optional: true, Desc: "Adapter name to bind to, e.g. \"eth0\". Only required when 'proto' is tls-in-icmp."},
		"tunnels[].adapter.mtu":                        {Default: fmt.Sprint(DefaultMTU), Desc: "Maximum Transmission Unit."},
		"tunnels[].adapter.queue_size":                 {Default: fmt.Sprint(DefaultQueueSize), Optional: true, Desc: "Size of the network queues for the tunnel."},
		"tunnels[].adapter.tunnel_address":             {Desc: "CIDR format."},
		"tunnels[].auth":                               {Optional: true, Desc: "Authentication configuration."},
		"tunnels[].auth.jwt":                           {Optional: true, Desc: "JWT authentication configuration."},
		"tunnels[].auth.jwt.public_key_source":         {Desc: "PublicKeySource is the path to the public key file used to verify peer JWT tokens."},
		"tunnels[].auth.jwt.token":                     {Desc: "Token is the JWT token presented to the peer during authentication."},
		"tunnels[].auth.mtls":                          {Optional: true, Desc: "MTLS authentication configuration."},
		"tunnels[].auth.mtls.cert_file":                {Optional: true, Desc: "CertFile is the client certificate presented by connect tunnels."},
		"tunnels[].auth.mtls.key_file":                 {Optional: true, Desc: "KeyFile is the private key for CertFile."},
		"tunnels[].auth.mtls.trust_pem":                {Optional: true, Desc: "TrustPEM is the path to the trust bundle for MTLS, if not provided, the system CA will be used. In client mode, this is used to validate the server certificate. In server mode, this is used to validate client certificates."},
		"tunnels[].auth.oidc":                          {Optional: true, Desc: "OIDC authentication configuration."},
		"tunnels[].auth.oidc.callback_timeout_seconds": {Desc: "CallbackTimeoutSeconds bounds browser redirect wait (zero = oidc default)."},
		"tunnels[].auth.oidc.claim_matchers":           {Desc: "ClaimMatchers see pkg/auth/oidc.Config.ClaimMatchers."},
		"tunnels[].auth.oidc.client_id":                {Desc: "Client ID of the application."},
		"tunnels[].auth.oidc.client_secret":            {Desc: "Client secret of the application."},
		"tunnels[].auth.oidc.clock_skew_seconds":       {Desc: "ClockSkewSeconds is leeway for id_token exp/iat (zero = oidc default)."},
		"tunnels[].auth.oidc.disable_pkce":             {Desc: "Disable Proof Key for Code Exchange (PKCE)."},
		"tunnels[].auth.oidc.issuer_url":               {Desc: "Issuer URL of the provider."},
		"tunnels[].auth.oidc.redirect_path":            {Desc: "RedirectPath is used only when RedirectURL is empty (default in oidc is /oauth2/callback)."},
		"tunnels[].auth.oidc.redirect_url":             {Desc: "RedirectURL is the full OAuth redirect URI registered at the IdP (optional; loopback ephemeral port if empty)."},
		"tunnels[].auth.oidc.scopes":                   {Desc: "Scopes for the authorization request (optional; oidc package defaults apply when empty)."},
		"tunnels[].auth.oidc.skip_open_browser":        {Desc: "Skip opening the browser for the authorization code flow."},
		"tunnels[].auth.type":                          {Default: fmt.Sprint(AuthTypeNone), Optional: true, Desc: "One of \"none\", \"jwt\", \"oidc\"."},
		"tunnels[].connect":                            {Optional: true, Desc: "Connect target configuration. Use with NAT traversal disabled."},
		"tunnels[].connect.address":                    {Desc: "Remote address to connect to. Fqdn or IP address."},
		"tunnels[].connect.port":                       {Desc: "Target port to connect to."},
		"tunnels[].connect.src_port":                   {Desc: "Source port to use for the connection. Leave empty for random selection."},
		"tunnels[].direction":                          {Desc: "Tunnel direction, either \"connect\" or \"listen\"."},
		"tunnels[].ice":                                {Optional: true, Desc: "NAT traversal and path discovery configuration. To enable NAT traversal, set ICEConfig.Enabled=true."},
		"tunnels[].ice.enabled":                        {Default: fmt.Sprint(false), Optional: true, Desc: "Enable ICE based network path discovery."},
		"tunnels[].ice.protos":                         {Optional: true, Desc: "Protocols to use for candidate generation. Not needed when TunnelConfig.Proto is already set."},
		"tunnels[].ice.signaling":                      {Optional: true, Desc: "Signaling configuration."},
		"tunnels[].ice.signaling.connect_offset":       {Default: fmt.Sprint(DefaultConnectOffset), Optional: true, Desc: "Connect offset in milliseconds."},
		"tunnels[].ice.signaling.encryption_key":       {Optional: true, Desc: "Encryption key for the signaling messages."},
		"tunnels[].ice.signaling.mqtt":                 {Optional: true, Desc: "Optional MQTT broker configuration to use instead of the default one."},
		"tunnels[].ice.signaling.mqtt.address":         {Default: fmt.Sprint(DefaultICEBrokerAddress), Desc: "MQTT broker address in the form of mqtt://host:port or ssl://host:port."},
		"tunnels[].ice.signaling.mqtt.client_id":       {Optional: true, Desc: "MQTT client ID."},
		"tunnels[].ice.signaling.mqtt.password":        {Optional: true, Desc: "MQTT password."},
		"tunnels[].ice.signaling.mqtt.username":        {Optional: true, Desc: "MQTT username."},
		"tunnels[].ice.signaling.server_id":            {Desc: "Use a connection ID to identify the connection, instead of the mapped IP."},
		"tunnels[].ice.stun_address":                   {Optional: true, Desc: "Optional custom STUN server to use instead of the global config.STUNAddress."},
		"tunnels[].listen":                             {Optional: true, Desc: "Listen address to bind to. Use with NAT traversal disabled."},
		"tunnels[].listen.address":                     {Desc: "Local address to listen on."},
		"tunnels[].listen.port":                        {Desc: "Local port to listen on."},
		"tunnels[].name":                               {Desc: "Friendly name for the tunnel."},
		"tunnels[].proto":                              {Default: fmt.Sprint(DefaultICEProtocol), Optional: true, Desc: "Transport protocol to use for the tunnel."},
		"tunnels[].reconnect_interval":                 {Default: fmt.Sprint(DefaultRetryInterval), Optional: true, Desc: "Reconnect interval in seconds."},
		"tunnels[].routes":                             {Optional: true, Desc: "Routes to enforce once the tunnel is established."},
		"tunnels[].routes[].destination":               {Desc: "Destination CIDR to route. E.g. \"192.168.1.0/24\"."},
		"tunnels[].routes[].gateway":                   {Optional: true, Desc: "For \"static\" routes, the gateway IP address to use for the route."},
		"tunnels[].routes[].interface":                 {Optional: true, Desc: "For \"static\" routes, the interface name to use for the route."},
		"tunnels[].routes[].type":                      {Desc: "Type of route. One of \"tunnel\", \"bypass\", \"static\"."},
		"tunnels[].stream_count":                       {Default: fmt.Sprint(DefaultStreamCount), Optional: true, Desc: "Number of streams to open. Valid only with streamed protocols like QUIC. Zero means the number of CPU cores."},
		"tunnels[].tls":                                {Optional: true, Desc: "TLS configuration."},
		"tunnels[].tls.cert_file":                      {Optional: true, Desc: "CertFile is the local certificate presented by listen tunnels."},
		"tunnels[].tls.insecure":                       {Optional: true, Desc: "Insecure is the flag to skip TLS certificate verification."},
		"tunnels[].tls.key_file":                       {Optional: true, Desc: "KeyFile is the private key for CertFile."},
		"tunnels[].wred":                               {Optional: true, Desc: "WRED settings."},
		"tunnels[].wred.drop_probability":              {Default: fmt.Sprint(DefaultWredDP), Optional: true, Desc: "Probability for packets to be dropped on busy queues. Accepts decimals between 0 and 1."},
		"tunnels[].wred.threshold":                     {Default: fmt.Sprint(DefaultWredThresh), Optional: true, Desc: "Threshold for WRED as a fraction of the queue length. Accepts decimals between 0 and 1."},
		"tunnels[].wred.weight_factor":                 {Default: fmt.Sprint(DefaultWredWF), Optional: true, Desc: "Weight factor. Lower values give more weight to recent packets."},
	}
}
